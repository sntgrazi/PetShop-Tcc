{
    "functions": {
      "routes/*.php": {
        "runtime": "vercel-php@0.4.0"
      }
    },
    "routes": [
        { "src": "/(.*)", "dest": "/rotas/index.php" }
    ],
    "builds": {
        "routes/*.php": "composer install && composer run-script build"
    }
}